## linux 정리

```shell
$ pwd # 현재 디렉토리의 위치를 알려준다
$ ls # 현재 디렉토리 안의 파일, 폴더 확인
$ ls -a
$ ls -al # 하위 디렉토리 자세히 조회
$ ls -l # 하위 디렉토리 자세히 조회 # l은 list! 
$ mkdir [폴더 이름] # 디렉토리 생성
$ touch empty_file.txt # 비어있는 파일 생성
```

